node_modules/
dist/
build/
.react-router/
*.tsbuildinfo

# Cloudflare
.mf
.wrangler/
worker-configuration.d.ts
wrangler.demo.jsonc

# Secrets — never commit
.env
.dev.vars*
!.dev.vars.example
!.env.example

*.local
.DS_Store

# Playwright
test-results/
playwright-report/
tests/e2e/artifacts/
artifacts/release-gate/

# Visual-pass renders (scripts/shots.mjs). Working artefacts of a design
# review, regenerated on demand — the repo does not carry screenshots.
shots/
